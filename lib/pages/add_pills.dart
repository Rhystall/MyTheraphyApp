import 'package:flutter/material.dart';

class AddPillsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Pills'),
      ),
      body: const Center(
        child: Text("Halaman Add Pills"),
      ),
    );
  }
}
